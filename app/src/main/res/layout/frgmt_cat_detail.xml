<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:tools="http://schemas.android.com/tools"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:layout_gravity="center"
              android:gravity="center"
              android:orientation="vertical">

    <ImageView
        android:id="@+id/cat_photo"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:maxHeight="300dp"
        android:maxWidth="300dp"
        android:scaleType="centerInside"/>

    <TextView
        android:id="@+id/cat_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?android:textAppearanceLarge"/>

    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <EditText
            android:id="@+id/cat_photo_input"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:hint="Ссылку на фото сюда"/>

        <Button
            android:id="@+id/cat_photo_input_confirm"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/cat_photo_input"
            android:layout_toRightOf="@id/cat_photo_input"
            android:text="сохранить"
            tools:ignore="RtlHardcoded"/>
    </RelativeLayout>

</LinearLayout>